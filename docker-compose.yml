version: "3"

services:

  bookingservice:
    build: .
    environment:
      - BOOKING_MONGO_HOST=mongodb
      - BOOKING_MONGO_PORT=27017
      - BOOKING_MONGO_USERNAME=bookingservice
      - BOOKING_MONGO_PASSWORD=mongo_password
      - BOOKING_MONGO_DATABASE=bookings
      - HTTP_SERVER_LISTEN=:80
      - MESSAGE_BUS_HOST=rabbitmq
      - MESSAGE_BUS_PORT=5672
      - MESSAGE_BUS_USERNAME=bookingservice
      - MESSAGE_BUS_PASSWORD=rabbitmq_password
    ports:
      - "10090:80"
    depends_on:
      mongodb:
        condition: service_started
      rabbitmq:
        condition: service_healthy

  mongodb:
    image: mongo:4.4.4
    expose:
      - 27017
    environment:
      - MONGO_INITDB_ROOT_USERNAME=bookingservice
      - MONGO_INITDB_ROOT_PASSWORD=mongo_password
      - MONGO_INITDB_DATABASE=bookings
    # volumes:
    #   - ./db-data/mogno:/data/db

  rabbitmq:
    image: rabbitmq:3.12.7-alpine
    expose:
      - 5672
    healthcheck:
      test: rabbitmq-diagnostics -q ping
      interval: 10s
      timeout: 10s
      retries: 5
    environment:
      - RABBITMQ_DEFAULT_USER=bookingservice
      - RABBITMQ_DEFAULT_PASS=rabbitmq_password
    # volumes:
    #   -